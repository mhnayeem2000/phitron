#include<bits/stdc++.h>
using namespace std;
int main(){
    int num;
    cin >> num;
    vector<int> vt;
    int arr[num];
    for(int i = 0; i < num;i++){
        cin >> arr[i];
    }
    for(int i = 0; i < num;i++){
        for( int j = 0; j < num; j++){
            if( arr[i] != arr[j] ){

            }
        }
    }
    return 0;
}